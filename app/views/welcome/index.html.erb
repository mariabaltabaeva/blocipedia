
<div class="text">
   <h1>Factpedia</h1>
   <p>Factpedia is a multilingual, web-based, free encyclopedia based on a model of openly editable content; a wiki.</p>
   <p>
     <% if user_signed_in? %>
    <%= link_to "Register App", new_registered_application_path, class: 'btn btn-success' %>
    <% else %>
     <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary'%> or
    <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary' %> today!
    <% end %>
  </p>
</div>
